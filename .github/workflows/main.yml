name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Build
        run: |
          echo "Building..."
          npm install
          npm run-script index
        env:
          RAZORPAY_SECRET: ${{5nCkmfjFfRnzfmL6hwKDmip9}}
          RAZORPAY_KEY_ID: ${{rzp_test_eHkH3Gvo3PpSJ8}}

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          folder: public